# This file is autogenerated by the aliases tool.
# For more info about aliases type `aliases --help`
# or visit https://github.com/sebglazebrook/aliases

---
run-tests:
  command: build && run bundle exec rspec spec
build:
  command: docker build -t super-simple-compiler-image .
run:
  command: docker run -ti -v "${PWD}":/code super-simple-compiler-image
